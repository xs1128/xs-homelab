services:
  whatsupdocker:
    image: getwud/wud
    container_name: wud
    environment:
      - WUD_SERVER_PORT=3001
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    ports:
      # original 3000 but clash wif grafana
      - 3001:3001

